# Flask Backend Configuration
SECRET_KEY=your-secret-key-change-in-production
PORT=8090
DEBUG=False

# Database Configuration (MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=order_user
DB_PASSWORD=order_pass_123!
DB_NAME=order_tracker

# Admin Password Hash (generate using init_db.py)
# Run: python -c "from bcrypt import hashpw, gensalt; print(hashpw(b'admin123', gensalt()).decode('utf-8'))"
ADMIN_PASSWORD_HASH=


# File Upload Configuration
UPLOAD_FOLDER=uploads

# Caching Configuration
CACHE_ENABLED=true
CACHE_TTL_FILE=300               # 5 minutes (file content)
CACHE_TYPE=database              # "database" or "memory" (future: "redis")
